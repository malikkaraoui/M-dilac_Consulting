{
    "rules": {
        "contacts": {
            ".read": false,
            ".write": true,
            "$contactId": {
                ".validate": "newData.hasChildren(['nom', 'prenom', 'email', 'message']) && newData.child('message').val().length <= 200"
            }
        }
    }
}